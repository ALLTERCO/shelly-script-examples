# HTTP Notify On Power Threshold

Script: `http-notify-on-power-threshold.shelly.js`

Monitors switch power consumption and sends an HTTP notification when power exceeds a configured threshold during an active time window.